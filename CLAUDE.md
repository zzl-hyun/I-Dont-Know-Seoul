# CLAUDE.md

이 파일은 새 세션이 맥락을 빠르게 잡기 위한 것입니다. 사용 방법·데이터 출처·
설계 근거는 [README.md](./README.md) 에 있으니 중복해서 적지 않습니다.
여기에는 **다시 논의할 필요가 없는 결정**과 **모르면 조용히 틀리는 것**만 적습니다.

---

## 확정된 제품 결정

사용자가 명시적으로 고른 것들입니다. 되묻지 말고 이 전제 위에서 작업하세요.

| 항목 | 결정 | 탈락한 대안 |
|---|---|---|
| 평판 산정 | 공공데이터 자동 지표 (치안 40 / 가격 35 / 편의 25) | 사용자 리뷰, 수동 큐레이션 |
| 지역 단위 | 행정동 427개 | 자치구 25개, 지하철 역세권 |
| 서비스 범위 | 서울시만 | 수도권, 전국 |
| 목적지 입력 | 자유 주소·장소 검색 (Kakao) | 역 선택, 큐레이션 목록 |
| 등급 설명 | 계산 과정까지 전면 공개, 접이식 | 수치 맥락만, 요약 한 줄만 |
| 통근 근거 | 경로 텍스트 + 지도에 점선 | 시간·환승 횟수만 |
| 설명 노출 | 상세 패널 + 지도 툴팁 | 상세 패널만 |
| 지하철 | 노선도 전체, 기본 ON + 토글 | 역 점만, 항상 켜기 |
| 다중 목적지 | 공통 한계 + **max 판정**, 최대 3곳 | 목적지별 한계, 평균/합 판정 |
| 버스 지표 | 교통 총합 0.25 안에서 **지하철 7 : 버스 3** | 5번째 독립 지표, 중첩 지표 |
| 화이트 모드 | 배경지도까지 전부 | 사이드바만 라이트 |
| 도보 가중치 | 역 선택 비교에서 도보 1분=1.4분 환산 (표시 시간은 실측 유지) | 가중치 없이 실측 그대로 비교 |
| 치안 축 가중치 | CCTV 0.35(비활성 유지) · 유흥업소 0.25 · 5대범죄 0.3 · 교통사고 0.1 | 유흥업소 단일 지표 유지 |
| 편의 축 POI 출처 | 소상공인 상가업소 (편의점·음식점·의료) | OSM POI 유지, 두 소스 합산 |

**등급은 상대 평가입니다.** 상위 30% Best / 하위 30% Bad. 절대 컷으로 바꾸자는
제안이 나오면 거절하세요 — 가중치를 바꾸는 순간 분포가 통째로 이동해서 대부분이
한 등급으로 몰립니다.

---

## 건드리면 깨지는 것

### `wrangler.jsonc` 의 `name`
그대로 workers.dev 서브도메인이 됩니다. 바꾸면 **배포 주소가 바뀝니다.**
현재 `i-dont-know-seoul`. 주소에서 계정 식별자를 빼는 건 대시보드에서 계정
서브도메인을 바꾸는 방식으로 이미 처리했습니다.

### `.gitignore` 의 `/dist/`
앞 슬래시가 **필수**입니다. 없으면 `data/dist/` 까지 무시되어 파이프라인
산출물이 빠지고, 클론한 저장소에서 테스트가 깨집니다.

### `src/lib/constants.ts` 의 통근 모델 상수
바꾸면 **반드시 `npm test`** 를 돌리세요. 2호선 순환 일주 87분을 앵커로
캘리브레이션되어 있고, 스팟체크 7개가 ±25% 안에 들어야 합니다.

`WALK_DETOUR_FACTOR` 는 **서울 보행 도로망 실측값(1.4)** 입니다. 감으로 찍은
값이 아니니 근거 없이 되돌리지 마세요. `npm run data:calibrate-walk` 가 OSM
보행 도로망(노드 89.7만) 위에서 1,686쌍을 재서 뽑습니다 — 근거는
`src/lib/constants.ts` 주석에 있습니다. **이 값은 `scripts/lib/geo.mjs` 에도
복제돼 있습니다**(.mjs 가 TS 를 못 읽어서). 한쪽만 바꾸면 화면 통근시간과
"최근접역 도보" 지표가 서로 다른 계수로 계산되는데 둘 다 그럴듯해서 눈으로는
못 잡습니다 — `commute.test.ts` 의 "도보 모델 상수가 앱과 파이프라인에서
같다" 가 이걸 막습니다. 바꾸면 `npm run data:metrics && npm run data:score`
까지 다시 돌려야 지표가 따라옵니다.

`WALK_SELECTION_WEIGHT` 는 예외입니다 — 역 **선택**(어느 역에서 타고 내릴지)
비교에만 쓰이고, 화면에 나오는 총 소요시간·구간 텍스트는 항상 가중치 없는
실측값입니다(`src/lib/dijkstra.ts` 의 `dist`=선택용 / `real`=실측용 분리).
이 상수를 바꿔도 스팟체크(승강장→승강장 실측 대조)엔 영향이 없습니다 —
스팟체크는 역 선택이 아니라 두 역 사이 순수 주행시간만 검증하기 때문입니다.
바뀔 수 있는 건 "동별 통근 계산"/"통근 경로 복원" 테스트 쪽 결과값입니다.

### 백분위는 파이프라인 값을 씁니다
`4-score.mjs` 가 계산한 `pct` 배열을 그대로 쓰세요. 클라이언트에서 다시
계산하면 동점 평균 순위 처리 때문에 축 점수와 미세하게 어긋나고, 화면에
표시되는 계산식이 실제와 안 맞게 됩니다.

반대로 **분포 통계(중앙값·동점 비율)는 클라이언트에서 계산합니다.**
모든 동의 원지표가 이미 번들에 있어서 파이프라인에 또 두면 이중 관리가 됩니다.

### MapLibre 표현식
`["zoom"]` 을 쓰는 `interpolate` 는 속성 표현식의 **최상위**에만 올 수 있습니다.
`["case", ..., ["interpolate", ..., ["zoom"], ...], 0]` 처럼 감싸면 레이어 추가
시 예외가 나고 **이후 초기화가 통째로 중단됩니다.** MapLibre는 이걸 `error`
이벤트로만 알리므로 지도가 그냥 빈 화면이 됩니다 (실제로 겪었고, `MapView.tsx` 에
error 리스너를 붙여둔 이유입니다).

### 지도 레이어 순서
```
dong-fill → dong-outline → subway-line → subway-hit → subway-station
→ dong-icon → dong-label → route-line-casing → route-walk-line → route-line
→ subway-label → dest-marker
```
`route-line-casing` 은 경로선 아래 깔리는 배경색 테두리입니다.
1호선 남색처럼 어두운 노선색과 파란 경로 점선이 겹치면 안 보이는 문제 때문에
추가했고, 라벨 halo와 같은 색(`MAP_THEME[...].halo`)을 재사용합니다.

경로선이 **두 레이어로 갈려 있습니다**(`route-walk-line` = 도보,
`route-line` = 지하철). MapLibre 의 `line-dasharray` 는 data-driven 표현식을
못 받아서 한 레이어 안에서 점선 모양을 구간별로 다르게 줄 수 없기 때문입니다.
피처의 `kind` 속성으로 `filter` 를 걸어 나눕니다. **경로 선분을 추가할 때는
`properties.kind` 를 반드시 실어야 합니다** — 없으면 어느 레이어에도 안 걸려
조용히 안 그려집니다.
지하철은 **등급 아이콘 아래**입니다. 주인공은 등급이고 지하철은 배경이라,
역 점이 등급 아이콘을 가리면 주 기능이 손상됩니다.

### 테마를 바꾸면 지도 레이어가 전부 날아갑니다

`setStyle` 로 배경지도를 갈아끼우면 MapLibre 가 커스텀 소스·레이어를 **보존하지
않습니다.** 그래서 `MapView.tsx` 에 두 장치가 있습니다.

- `installLayers()` — 소스·레이어 설치를 함수로 뽑아 `styledata` 이후 다시 부른다
- `styleEpoch` — `feature-state` 도 함께 날아가는데, 상태 반영 effect 들은
  `readyRef` 가 true 면 즉시 적용하고 끝나 자기 힘으로는 다시 안 걸린다.
  이 카운터를 의존성에 넣어 재실행시킨다.

**지도에 레이어나 상태 반영 effect 를 추가하면 이 두 곳도 같이 손봐야 합니다.**

---

## 조용히 틀리는 함정

이미 한 번씩 물렸던 것들입니다.

**공공데이터포털 인증키는 "Decoding" 값**
`%2B`·`%3D` 가 든 Encoding 값을 넣으면 스크립트가 다시 URL 인코딩해서 이중
인코딩이 되고 **전부** 인증 실패합니다. 오류 메시지가 친절하지 않습니다.

**실거래가 API는 동시 요청 2개까지**
6개로 돌렸더니 약 110건째부터 600건 끝까지 전부 실패했습니다. 지금은 동시성 2 +
요청 간 120ms + 지수 백오프로 실패 0건입니다. 속도 올리지 마세요.

**Kakao는 키 발급만으로 안 됩니다**
제품 설정 → 카카오맵 → 활성화 ON 이 따로 필요합니다.
`disabled OPEN_MAP_AND_LOCAL service` 가 그 신호입니다.

**OSM 역명의 괄호 부기명을 떼야 합니다**
`"대림"` 과 `"대림(구로구청)"` 이 따로 태깅되어 있어서, 안 떼면 같은 역이
갈라지고 **환승이 끊깁니다.** 대림(2↔7), 잠실(2↔8), 교대(2↔3), 왕십리(4개 노선)가
실제로 끊겼었습니다. 경로는 나오되 멀리 돌아가서 눈에 잘 안 띕니다.
`2-subway.mjs` 의 검증이 알려진 환승역을 검사합니다.

**실거래가는 법정동 기준입니다**
행정동에 바로 안 붙습니다. 번호를 벗겨 매칭해서 427개 중 344개(81%)만 직접
붙고 나머지는 자치구 중앙값입니다. 같은 법정동의 행정동들이 같은 월세 값을
갖는 건 원 데이터 한계이지 버그가 아닙니다.

**서울 CCTV 데이터셋은 폐기됐습니다**
"안심이 CCTV 연계 현황"(OA-20923)이 2026-02-10 자로 내려갔습니다. API가
ERROR-500 만 줍니다. OSM `man_made=surveillance` 도 서울 875개(실제 8만대의 1%)라
못 씁니다. **다시 시도하지 마세요.** (치안 축은 대신 5대범죄·교통사고 다발지역으로
보강했습니다 — `cctvPerKm2` 자리는 향후를 위해 비워둔 채로 둡니다.)

**`compatibility_date`**
설치된 wrangler의 workerd 런타임이 지원하는 범위 안이어야 합니다. 미래 날짜를
넣으면 `wrangler dev` 가 기동 자체를 못 합니다.

**정부 CSV는 Windows 줄바꿈(`\r\n`)입니다**
각 줄 마지막 필드에 `\r` 이 붙습니다. 숫자는 `Number()`가 앞뒤 공백을 무시해서
문제없지만, 문자열(자치구명 등)로 다른 데이터와 조인하면 마지막 컬럼 하나만
안 맞아서 **그 값만 조용히 빠집니다.** 5대범죄 CSV에서 강동구가 이렇게
빠졌었습니다 — 필드마다 `trim()` 하세요.

**정부 CSV는 CP949(EUC-KR 상위호환) 인코딩이 흔합니다**
`utf8`로 그대로 읽으면 한글이 깨져서 조인이 전부 실패하는데 에러 메시지가
친절하지 않습니다. 이 프로젝트는 원본을 받는 시점에 서울분만 UTF-8로 뽑아
`data/raw/`에 커밋해서, 파이프라인 자체는 인코딩을 신경 쓰지 않게 했습니다.

**상가업소의 주점을 음식점에 넣으면 이중 계산이 됩니다**
소상공인 업종분류에서 주점(`I211`, 서울 16,120곳)은 **음식(`I2`) 밑에** 있습니다.
그대로 음식점 지표에 넣으면 같은 업소를 편의 축에서 가점하고 치안 축의 유흥업소
밀도로 감점하게 됩니다. `SBIZ_GROUPS.food` 가 `I211` 을 빼는 이유입니다.
약국(`G21501`)은 반대로 원 분류가 **소매**인데 의료 지표에 넣습니다 — 재는 게
"의료 접근성"이기 때문입니다.

**공공데이터포털은 API마다 별도로 활용신청해야 합니다**
같은 계정·같은 `DATA_GO_KR_KEY`라도 API(예: 단독/다가구, 오피스텔, 아파트
실거래가)마다 개별 승인이 필요합니다. 안 된 API를 부르면
`SERVICE_KEY_IS_NOT_REGISTERED_ERROR`("등록되지 않은 서비스키")가 옵니다 —
URL이 틀렸다는 신호가 아니라 그 API만 활용신청이 안 됐다는 신호입니다.

---

## 명령

```bash
npm run cf:dev      # Worker + 정적자산 통합 → localhost:8787 (실환경에 가장 가까움)
npm test            # 75개 — 통근 정확도 / 등급 설명 / 노선도 / URL 공유
npm run typecheck
npm run cf:deploy   # 빌드 + 배포
npm run data:seed   # KV 스냅샷 갱신 (데이터만 바뀐 경우 이것만)

npm run data:calibrate-walk   # WALK_DETOUR_FACTOR 실측 (일회성, 파이프라인 아님)
```

`data:calibrate-walk` 는 Overpass 에서 보행 도로망 33타일(약 310MB)을
`data/raw/walk-tiles/` 에 캐시합니다. 두 번째 실행부터 네트워크를 안 씁니다.
결과는 사람이 읽고 `src/lib/constants.ts` 에 옮겨 적습니다 — 자동 반영하지
않습니다. 계수가 바뀌면 통근시간 분포가 통째로 움직여서, 눈으로 보고
결정할 문제이기 때문입니다.

데이터 파이프라인은 `1-boundaries → 2-subway → 3-metrics → 4-score` 순서이며,
각 스크립트가 자체 검증을 내장하고 실패 시 종료 코드 1을 반환합니다.
`data/raw/` 는 캐시라 지우면 다시 받습니다.

**번들을 바꿨으면 `npm run data:seed` 를 잊지 마세요.** `/api/data` 는 KV를 먼저
보므로, 시드하지 않으면 배포해도 옛 데이터가 나갑니다.

키는 `.env`(파이프라인, gitignore)와 `wrangler secret`(Worker)에 있습니다.
**저장소가 공개이므로 키를 커밋하지 마세요.**

---

## URL 에 싣는 것과 안 싣는 것

목적지·통근한계·가중치·예산·지도 모드·꺼둔 노선은 URL 에 담습니다(`shareUrl.ts`).
**테마는 담지 않습니다** — 링크를 받은 사람의 취향을 덮어쓸 이유가 없습니다.
기본값은 싣지 않아 기본 상태로 공유하면 쿼리스트링이 빕니다.

받은 링크를 믿지 않습니다. 서울 근방 밖 좌표는 목적지로 받지 않고, 범위를 벗어난
숫자는 기본값으로 되돌립니다.

---

## 아직 안 된 것

- **CCTV 밀도 미반영** — 위 사유로 불가, 다시 시도하지 말 것. 5대범죄(자치구
  단위)·교통사고 다발지역(동 단위)으로 대신 보강했습니다
- **급행 미반영** — 9호선·1호선 급행 등은 완행 기준으로만 계산
- **환승 도보시간이 상수 2분** — OSM에 실제 값이 없음
- **버스는 정류장 밀도만** — 노선 다양성은 못 봄. 정류장→노선 매핑(1,005개 노선)이
  필요해 비용이 큼
- **대형마트·백화점 미반영** — 소상공인 데이터는 대기업 점포를 뺀다. 편의점·마트
  지표(`G204` 종합소매)에 이마트·롯데마트·백화점이 없다
- **유흥업소는 아직 OSM** — 소상공인에 `일반·무도 유흥주점` 2,484곳이 따로 있어
  지표 이름("유흥업소")에는 그쪽이 의미상 더 정확하다. 치안 축이라 영향 범위가
  달라 별도로 판단할 것
- **버스 노선도는 그리지 않음** — 노선 1,005개·정류장 14,626개라 지도가 스파게티가
  된다. 통근 계산에 넣는 것도 배차·교통상황 데이터가 없어 검증 불가. 다시 시도하지 말 것

## 확인된 것

- 지도 렌더, 지하철 노선도(투명도 0.85), 통근 경로 점선 — 사용자 브라우저에서
  육안 확인 완료
- 프로덕션 `/api/data` 가 KV에서 서빙 중 (`X-Oneday-Source: kv`)
- 월세 427개 동 전부 수록, Kakao 지오코딩 동작
- 아파트·5대범죄·교통사고 반영한 파이프라인 실행 확인(2026-08-10) — 실거래
  217,441건(직접 매칭 344/427동), 5대범죄 25개 자치구, 교통사고 3742/3743
  지점 배정. 안전 축 가중치 재분배 후 등급이 넘나든 동 170/427개, 그중
  Best↔Bad로 극단적으로 바뀐 동은 8개뿐 — 전부 설명 가능한 방향(신촌·연희동은
  유흥업소 단일지표의 왜곡이 실제 범죄·사고 데이터로 교정됨)

---

## 작업 방식

- 사용자는 한국어로 대화합니다. 코드 주석과 커밋 메시지도 한국어입니다.
- 커밋은 **작업 순서대로 쪼개서** 합니다. 한 번에 몰아 담지 마세요.
- 커밋 본문에는 "무엇을" 보다 **"왜 그렇게 했는지"** 를 적습니다. 특히 겪었던
  버그와 그 원인을 남깁니다.
- 수치를 문서에 적을 때는 **실제 산출물과 대조**한 값만 씁니다.
